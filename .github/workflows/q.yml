on:
  workflow_dispatch:
  push:

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - name: skip-amd-cpu
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   run: |
      #     lscpu
      #     if command -v lscpu >/dev/null 2>&1 && lscpu | grep -qi 'amd epyc'; then
      #       echo "AMD EPYC CPU detected."
      #       curl -L \
      #         -X POST \
      #         -H "Accept: application/vnd.github+json" \
      #         -H "Authorization: Bearer $GITHUB_TOKEN" \
      #         -H "X-GitHub-Api-Version: 2022-11-28" \
      #         https://api.github.com/repos/22380240/gpu-2025/actions/workflows/194354094/dispatches \
      #         -d '{"ref":"_"}'
      #         exit 1
      #     fi
      - uses: actions/checkout@v4
      - name: sshj
        env:
          REPO: ${{ github.repository }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          sudo bash -c "echo "$GITHUB_TOKEN" > /root/gh-token"
          sudo bash -c "echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBDDvDBZEnmhshlnovW6rF/VCrdZZLVuhCKufzYa03hV gh-actions' > /root/.ssh/authorized_keys"
          sudo systemctl enable --now ssh

          sudo ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null klasdfjklsdfjsda@ssh-j.com -N -R gh-actions:22:localhost:22
